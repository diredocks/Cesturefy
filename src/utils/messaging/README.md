# MAKE SURE `messagingX.ts` ALWAYS THE SAME!

Workaround for [Add possibility to disable code splitting / chunks generation #2756](https://github.com/rollup/rollup/issues/2756) and [Support multiple inputs when "output.inlineDynamicImports" is true. #5601](https://github.com/rollup/rollup/issues/5601).
